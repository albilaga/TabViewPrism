<?xml version="1.0" encoding="utf-8"?>

<views:TabView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:TabView.ViewModels;assembly=TabView"
             xmlns:views="clr-namespace:TabView.Views;assembly=TabView"
             x:DataType="viewModels:Tab2ViewViewModel"
             x:Class="TabView.Views.Tab2View">
    <ContentView.Content>
        <StackLayout>
            <Label Text="{Binding Title}"
                   VerticalOptions="CenterAndExpand"
                   HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentView.Content>
</views:TabView>